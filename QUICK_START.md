# دليل البدء السريع - نظام إدارة الصحة

## 🚀 **البدء في 5 دقائق**

### 1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd company-hospital-health-system
```

### 2. **تثبيت التبعيات**
```bash
npm install
```

### 3. **إعداد Supabase**
1. اذهب إلى [supabase.com](https://supabase.com)
2. أنشئ مشروع جديد
3. انسخ `URL` و `anon key` من Settings > API
4. انسخ محتوى `supabase_schema.sql` وقم بتنفيذه في SQL Editor

### 4. **إعداد متغيرات البيئة**
```bash
cp .env.local.example .env.local
```

عدّل ملف `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 5. **تشغيل المشروع**
```bash
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000)

## 🎯 **الخطوات التالية**

### **للشركات:**
1. سجل شركة جديدة من الصفحة الرئيسية
2. أضف موظفين من لوحة التحكم
3. احجز مواعيد طبية للموظفين
4. تابع السجلات الطبية

### **للمستشفيات:**
1. سجل مستشفى جديد من الصفحة الرئيسية
2. أضف أطباء وممرضين
3. استقبل المواعيد من الشركات
4. أدر الملفات الطبية والفوترة

## 📱 **المميزات المتاحة حالياً**

### ✅ **مكتمل:**
- ✅ تسجيل الشركات والمستشفيات
- ✅ نظام المصادقة الآمن
- ✅ لوحات تحكم للشركة والمستشفى
- ✅ إضافة وإدارة الموظفين
- ✅ نظام الصلاحيات والأمان

### 🔄 **قيد التطوير:**
- 🔄 نظام المواعيد
- 🔄 إدارة الإجازات المرضية
- 🔄 نظام الفحوصات الطبية
- 🔄 نظام الفوترة
- 🔄 التقارير والإحصائيات

## 🛠️ **أدوات التطوير**

### **التشغيل:**
```bash
npm run dev          # تشغيل في وضع التطوير
npm run build        # بناء للإنتاج
npm run start        # تشغيل في وضع الإنتاج
```

### **الاختبار:**
```bash
npm run lint         # فحص الكود
npm run test         # تشغيل الاختبارات
npm run type-check   # فحص الأنواع
```

### **التنسيق:**
```bash
npm run lint:fix     # إصلاح أخطاء التنسيق
```

## 📁 **هيكل المشروع**

```
src/
├── app/                    # صفحات التطبيق
│   ├── auth/              # صفحات المصادقة
│   ├── dashboard/         # لوحات التحكم
│   ├── employees/         # إدارة الموظفين
│   └── ...
├── components/            # مكونات React
│   ├── ui/               # مكونات UI أساسية
│   └── ...
├── lib/                  # مكتبات مساعدة
│   ├── supabase.js       # تكوين Supabase
│   └── utils.js          # دوال مساعدة
└── ...
```

## 🔐 **الأمان**

- **Row Level Security (RLS)** مفعل على جميع الجداول
- **مصادقة آمنة** مع Supabase Auth
- **صلاحيات محددة** لكل نوع مستخدم
- **حماية البيانات** الطبية الحساسة

## 📞 **الدعم**

- 📧 البريد الإلكتروني: support@example.com
- 💬 Discord: [رابط السيرفر]
- 📖 التوثيق: [رابط التوثيق]
- 🐛 الإبلاغ عن الأخطاء: [GitHub Issues]

## 🎉 **معلومات إضافية**

- **اللغة:** العربية (RTL)
- **الإطار:** Next.js 15 + React 19
- **قاعدة البيانات:** PostgreSQL (Supabase)
- **التصميم:** Tailwind CSS
- **التحقق:** Zod + React Hook Form

---

**استمتع بالتطوير! 🚀**